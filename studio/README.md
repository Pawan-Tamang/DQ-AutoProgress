# DQ Lobby Manager

Authorized Roblox Studio system for dungeon-party coordination.

This is **not** an executor script. Privileged lobby actions run on the server.
The client only displays UI and sends requests. The server validates everything.

See repo folder `studio/` for the full module tree.
